# Paths tracked against upstream cyverse/ds-collection for security-relevant
# changes. See scripts/check-upstream.sh.
#
# This fork intentionally diverges from upstream in large ways (iRODS pinned
# to 4.3.3, Ubuntu 24 support, testing/infra rewrites), so a full merge from
# upstream is not viable. These are the paths where upstream changes are
# still worth reviewing and manually porting.

playbooks/files/irods/etc/irods/cve.re

# Deploys/hardens the same CVE workarounds (e.g. msiExecCmd_bin permissions
# for the TAR slip fix) - changes here are usually paired with cve.re changes.
playbooks/irods_cfg.yml

# Test coverage for the two files above. Our testing infra has diverged too
# much from upstream's to port these verbatim, but worth reading when they
# change to make sure our own tests still cover the same scenarios.
playbooks/tests/rules/cve.py
playbooks/tests/irods_cfg.yml

# Trash removal, quota-usage tracking, and storage free-space tracking. Not
# security-relevant, but restored from a prior deletion (see
# cyverse_ds_housekeeping.md) - watched so future upstream changes don't
# silently drift again.
playbooks/files/irods/etc/irods/cyverse_housekeeping.re
playbooks/files/irods/etc/irods/cyverse_trash.re
playbooks/tests/rules/cyverse_housekeeping.py
playbooks/tests/rules/cyverse_trash.py

# cyverse_core.re dispatches to cyverse_trash_api_* (see above) via pep_api_*
# wrappers for coll_create/data_obj_copy/data_obj_put/data_obj_rename/
# data_obj_unlink/data_obj_create/rm_coll. This fork's core.re predates
# upstream's encryption/repl/transfer_tracking dispatch calls in these same
# hooks and intentionally only carries the trash ones - diff carefully rather
# than porting upstream's version of these hooks wholesale.
playbooks/files/irods/etc/irods/cyverse_core.re
